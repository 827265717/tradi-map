# 复制为 .env 并填写真实值（.env 已在 .gitignore 中）
DATABASE_URL=postgres://tradimap:yourpassword@127.0.0.1:5432/tradimap?sslmode=disable
JWT_SECRET=替换为一个长随机字符串，可用：openssl rand -base64 32
PORT=8080
# 多个来源用逗号分隔
ALLOWED_ORIGINS=http://localhost:5173,https://tradi-map.vercel.app
